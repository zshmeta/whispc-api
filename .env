# .env file

# Port configuration
export PORT="${PORT:-9000}"

# Whisper-ctranslate2 settings
export WHISPER_MODEL="${WHISPER_MODEL:-small}"
export WHISPER_OUTPUT_FORMAT="${WHISPER_OUTPUT_FORMAT:-json}"
export WHISPER_WORD_TIMESTAMP="${WHISPER_WORD_TIMESTAMP:-true}"
export WHISPER_DEVICE="${WHISPER_DEVICE:-cpu}"
